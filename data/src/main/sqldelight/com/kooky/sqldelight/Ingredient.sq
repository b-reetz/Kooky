CREATE TABLE ingredient (
    id INTEGER NOT NULL UNIQUE PRIMARY KEY,
    name TEXT NOT NULL
);

selectAll:
SELECT * FROM ingredient ORDER BY name;

insertOrReplace:
INSERT OR REPLACE INTO ingredient(name) VALUES (?);

-- insertOrReplace:
-- INSERT OR REPLACE INTO ingredient(name) VALUES (?);

selectByName:
SELECT * FROM ingredient WHERE name = ?;

empty:
DELETE FROM recipe;

deleteById:
DELETE FROM recipe WHERE id = ?;

deleteByName:
DELETE FROM recipe WHERE name = ?;